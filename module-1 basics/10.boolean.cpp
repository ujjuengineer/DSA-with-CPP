#include<iostream>
using namespace std;
int main (){
    bool flag = true; // if we put
    cout<< flag << endl;
    // true means 1 and false means 0
    cout<< (7<3); // it means we are asking, Is 7 is less than 3, its false hence compiler will give false i.e., 0
}