/*
create a class 'book' with name, price and number of pages as its attributes.

The class should contain following member functions :
    
    countBook(int price) : the function will return count of all the books that have a price less than the given price
    
    isBookPresent(string title) : this will return a boolean value indicating wether any book with the given title exist or not
*/

#include<iostream>
using namespace std;

class book {

    public : 
    string name;
    int price;
    int NumOfPages;

    // error in question, not clear
}