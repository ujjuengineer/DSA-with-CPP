#include<iostream>
using namespace std;
int main(){
    cout<<"enter an integer ";
    int n;
    cin>>n;
    // if(n<0){
    //     n = -n;
    // }
    // cout<< n;

    // we can even use inbuit function to find the absolute value i.e., abs()
    cout<< abs(n);
}