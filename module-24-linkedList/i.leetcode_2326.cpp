/*
basically we have given a head of a linkedlist, we have to fit the nodes of this
linkedlist in spiral matrix and then return this matrix

algo : 
    use 4 pointer minRow, minCol, maxRow and maxCol to iterate on the matrix in
    spiral order and fill the value of nodes in it
*/