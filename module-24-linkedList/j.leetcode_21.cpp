/*
Mearge two sorted array : 
    we have given 2 sorted linked list, we have to merge them in sorted order. 

    create a node temp and store that temp in another new node c, now 
    keep adding the nodes of a and b depending on the value of the nodes

    and finally when a become null, then add remaining b in that, and when b become
    null then add remaining a in that. and return c ka next
*/